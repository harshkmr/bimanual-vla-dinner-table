# Freeze Changelog (N4+N5) — auditor-visible commit trail.

- Init: V3.6 filed; freeze date TBD.
- N2 kill-switch deadline: TBD-calendar-date (freeze day −2). If no SmolVLA checkpoint under `weights/smolvla/` by then, log the one-line ACT-primary re-route here + update `rubric_trace.md` (never a silent edit).
- N5: `eval/eval_10_seeds.py` deletion will be listed here with the freeze commit hash (pending).
