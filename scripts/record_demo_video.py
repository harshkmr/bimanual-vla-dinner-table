# TICKET-01 stub — headless 4-view composite + HUD (logic lands in TICKET-08).
"""--output output/demo_10_seeds.mp4 --seeds 2 --steps 210; VideoWriter mp4v, no imshow."""
TODO_TICKET = "TICKET-08"

# N6 — HUD footer seed string (grep-checkable, kept verbatim).
HUD_FOOTER = "liquid=FILL meter (kinematic proxy, not fluid sim)"


def main():
    raise NotImplementedError("TICKET-08")


if __name__ == "__main__":
    main()
